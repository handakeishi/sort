var sortNumber = function (number) {
   number.sort(function (c, d) {
       if (c == d) {
           return 0;
       }
       return c < d ? -1 : 1;
   });
};

var number = [19, 3, 81, 1, 24, 21];
sortNumber(number);
console.log(number);
